{
  "description": "Trim a video with overlapping markers",
  "request": "Trim the video to start at 10 seconds and end at 30 seconds, then add a title at 25 seconds",
  "expected_outcome": "A trimmed video with in marker at 10 seconds and out marker at 30 seconds, and a title at 25 seconds",
  "constraints": {
    "in_marker": {"min": 0, "max": 100},
    "out_marker": {"min": 0, "max": 100},
    "in_before_out": true,
    "title_time": {"min": 0, "max": 100}
  }
}