{
  "description": "Trim a video with invalid out marker",
  "request": "Trim the video to start at 10 seconds and end at 150 seconds",
  "expected_outcome": "An error response due to invalid out marker",
  "constraints": {
    "in_marker": {"min": 0, "max": 100},
    "out_marker": {"min": 0, "max": 100},
    "in_before_out": true
  }
}