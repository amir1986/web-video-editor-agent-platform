{
  "description": "Add a title with invalid time (too large)",
  "request": "Add a title 'Test' at 150 seconds",
  "expected_outcome": "An error response due to invalid title time",
  "constraints": {
    "title_text": {"min_length": 1, "max_length": 100},
    "title_time": {"min": 0, "max": 100}
  }
}