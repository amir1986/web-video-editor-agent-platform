{
  "description": "Trim a video with in marker after out marker",
  "request": "Trim the video to start at 30 seconds and end at 10 seconds",
  "expected_outcome": "An error response due to invalid marker order",
  "constraints": {
    "in_marker": {"min": 0, "max": 100},
    "out_marker": {"min": 0, "max": 100},
    "in_before_out": true
  }
}